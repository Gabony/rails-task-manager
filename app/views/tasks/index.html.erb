<h1>My tasks</h1>
<ul style="list-style: none;">
<% @tasks.each do |task| %>
  <li><input type="checkbox" id="scales" name="scales" <%= "checked" if task.completed %>><%= link_to task.title, task_path(task) %> . <%= link_to "🗑", task_path(task), method: :delete, data: {confirm: "Are you sure?"}%></li>
<% end %>
<%= link_to "+ Add a new task", "tasks/new" %>
